# 一、项目中目前的分库分表相关配置

## 1.1 ✅ 现有的基因法订单号生成（支持幂次方扩容）

```java
// SnowflakeIdGenerator.java#getOrderNumber
public synchronized long getOrderNumber(long userId, long tableCount, long databaseCount) {
    // 计算基因位数 = 表基因位数 + 库基因位数
    long tableGeneLength = log2N(tableCount);      // log2(4) = 2位
    long databaseGeneLength = log2N(databaseCount); // log2(2) = 1位
    long totalGeneLength = tableGeneLength + databaseGeneLength; // 3位
    
    // 从用户ID中提取后3位作为基因
    long geneMask = (1L << totalGeneLength) - 1;
    long userGene = userId & geneMask;
    
    // 订单号结构：[时间戳][数据中心][机器ID][序列号][基因3位]
    return ((timestamp - BASIS_TIME) << timestampLeftShift)
            | (datacenterId << datacenterIdShift)
            | (workerId << workerIdShift)
            | (sequence << totalGeneLength)
            | userGene;  // ⭐ 关键：低3位就是分片基因
}
```

### 优点：

订单号的低N位完全来自userId，确保了订单号和用户ID路由到相同的分片。

## 1.2 ✅ 位运算分片算法（标准幂次方扩容）

### 分表算法（正在使用）：

**位置：** `com.damai.shardingsphere.TableOrderComplexGeneArithmetic`

```java
actualTableNames.add(logicTableName + "_" + ((shardingCount - 1) & value));
```

### 分库算法（正在使用）：

**位置：** `com.damai.shardingsphere.DatabaseOrderComplexGeneArithmetic`

```java
return (databaseCount - 1) & (splicingKey >> tableGeneLength);
```

### 原理：

- 当 `shardingCount` 是2的幂次方时（如4），`shardingCount - 1 = 3 = 0b11`
- `(3 & value)` 等同于取value的**低2位**
- **这正是幂次方扩容的核心算法！**

# 二、扩容迁移的核心原理

## 2.1 核心原理：按位分裂

### 前提条件：

**表数量必须是2的幂次方**（2, 4, 8, 16, 32...）

### 原理：

使用哈希值的**低k位**决定表号：

```
初始：4表（2²），使用低2位
  hash = ...1101 → 后2位 = 01 → 表1

扩容到8表（2³），使用低3位
  hash = ...1101 → 后3位 = 101 → 表5
  
观察：
- 原表0（00）拆分为 → 新表0（000）+ 新表4（100）
- 原表1（01）拆分为 → 新表1（001）+ 新表5（101）
- 原表2（10）拆分为 → 新表2（010）+ 新表6（110）
- 原表3（11）拆分为 → 新表3（011）+ 新表7（111）

规律：每个原表拆分为2个新表，只需迁移一半数据！
```

## 2.2 位运算详解

### 初始状态：4表（2²）

```java
tableCount = 4;
tableMask = tableCount - 1 = 3 = 0b11;  // 低2位掩码

userId = 13 = 0b1101;
tableIndex = userId & tableMask = 0b1101 & 0b11 = 0b01 = 1;  // → 表1
```

### 扩容后：8表（2³）

```java
tableCount = 8;
tableMask = tableCount - 1 = 7 = 0b111;  // 低3位掩码

userId = 13 = 0b1101;
tableIndex = userId & tableMask = 0b1101 & 0b111 = 0b101 = 5;  // → 表5
```

### 迁移判断

```java
// 判断旧表1的数据是否需要迁移
int oldTableIndex = 1;           // 0b01
int newBit = userId & 0b100;     // 检查第3位（新增的那一位）

if (newBit == 0) {
    // 第3位=0 → 低3位=001 → 留在原表1
} else {
    // 第3位=1 → 低3位=101 → 迁移到新表5
}
```

## 2.3 分库分表组合扩容

### 当前配置：2库×4表（8物理分片）

```
订单号结构（64位）：
[41位时间戳][5位数据中心][5位机器][12位序列][3位基因]
                                            ↓
                                     [1位库基因][2位表基因]

路由算法：
- 表索引 = orderNumber & 0b11  （取后2位）
- 库索引 = (orderNumber >> 2) & 0b1  （跳过表基因，取第3位）
```

### 扩容到2库×8表（16物理分片）

```
订单号结构（64位）：
[41位时间戳][5位数据中心][5位机器][12位序列][4位基因]
                                            ↓
                                     [1位库基因][3位表基因]

路由算法：
- 表索引 = orderNumber & 0b111  （取后3位）
- 库索引 = (orderNumber >> 3) & 0b1  （跳过表基因，取第4位）
```

### 迁移策略

```
原表拆分规律：
- d_order_0 (0b00) → d_order_0 (0b000) + d_order_4 (0b100)
- d_order_1 (0b01) → d_order_1 (0b001) + d_order_5 (0b101)
- d_order_2 (0b10) → d_order_2 (0b010) + d_order_6 (0b110)
- d_order_3 (0b11) → d_order_3 (0b011) + d_order_7 (0b111)

迁移条件：
if ((orderNumber & 0b100) != 0) {
    // 第3位=1，需要迁移到新表
    targetTable = oldTable + 4;
}
```

### 当前的分库分表计算

```java
// 分表
actualTableNames.add(logicTableName + "_" + ((shardingCount - 1) & value));
// 4表时：(4-1) & value = 3 & value，取value的低2位

// 分库
return (databaseCount - 1) & (splicingKey >> tableGeneLength);
// 2库时：(2-1) & (value >> 2) = 1 & (value >> 2)，跳过表基因取库基因
```

# 三、新的分库分表算法

## 3.1 新的分库算法

**位置：** `com.damai.shardingsphere.powerof2.DatabaseOrderPowerOf2Algorithm`

```java
public class DatabaseOrderPowerOf2Algorithm implements ComplexKeysShardingAlgorithm<Long> {
    
    /**
     * 属性分库名
     * */
    private static final String SHARDING_COUNT_KEY_NAME = "sharding-count";
    
    /**
     * 属性分表名
     * */
    private static final String TABLE_SHARDING_COUNT_KEY_NAME = "table-sharding-count";
    
    /**
     * 表基因位数（用于跳过）
     */
    private int tableBitCount;
    
    /**
     * 库掩码
     */
    private long databaseMask;
    
    @Override
    public void init(Properties props) {
        //分库数量
        final int databaseCount = Integer.parseInt(props.getProperty(SHARDING_COUNT_KEY_NAME));
        //分表数量
        final int tableCount = Integer.parseInt(props.getProperty(TABLE_SHARDING_COUNT_KEY_NAME));
        // 验证是否为2的幂次方
        if (!isPowerOf2(databaseCount) || !isPowerOf2(tableCount)) {
            throw new IllegalArgumentException(
                String.format("库数和表数必须是2的幂次方，当前：库=%d, 表=%d", databaseCount, tableCount)
            );
        }
        // 计算表基因位数（用于跳过）
        this.tableBitCount = Integer.numberOfTrailingZeros(tableCount);
        
        // 计算库掩码
        this.databaseMask = databaseCount - 1;
    }
    
    @Override
    public Collection<String> doSharding(
        Collection<String> allActualSplitDatabaseNames,
        ComplexKeysShardingValue<Long> complexKeysShardingValue) {
        
        List<String> actualDatabaseNames = new ArrayList<>();
        Map<String, Collection<Long>> columnNameAndShardingValuesMap = 
            complexKeysShardingValue.getColumnNameAndShardingValuesMap();
        
        if (CollectionUtil.isEmpty(columnNameAndShardingValuesMap)) {
            return actualDatabaseNames;
        }
        
        Long shardingKey = getShardingKey(columnNameAndShardingValuesMap);
        
        if (Objects.nonNull(shardingKey)) {
            // ⭐ 核心：跳过表基因位，提取库基因
            long databaseIndex = (shardingKey >> tableBitCount) & databaseMask;
            String databaseIndexStr = String.valueOf(databaseIndex);
            for (String actualSplitDatabaseName : allActualSplitDatabaseNames) {
                //将所有的分库名和得到的分库索引进行匹配
                if (actualSplitDatabaseName.contains(databaseIndexStr)) {
                    actualDatabaseNames.add(actualSplitDatabaseName);
                    break;
                }
            }
            return actualDatabaseNames;
        }
        return allActualSplitDatabaseNames;
    }
    
    /**
     * 获取分片键（优先order_number，其次user_id）
     */
    private Long getShardingKey(Map<String, Collection<Long>> columnMap) {
        //order_number条件的值
        Collection<Long> orderNumberValues = columnMap.get("order_number");
        //user_id条件的值
        Collection<Long> userIdValues = columnMap.get("user_id");
        //如果是order_number查询
        if (CollectionUtil.isNotEmpty(orderNumberValues)) {
            return orderNumberValues.stream().findFirst()
                    .orElseThrow(() -> new DaMaiFrameException(BaseCode.ORDER_NUMBER_NOT_EXIST));
        } else if (CollectionUtil.isNotEmpty(userIdValues)) {
            //如果是user_id查询
            return userIdValues.stream().findFirst()
                    .orElseThrow(() -> new DaMaiFrameException(BaseCode.USER_ID_NOT_EXIST));
        }
        return null;
    }
    
    private boolean isPowerOf2(int n) {
        return n > 0 && (n & (n - 1)) == 0;
    }
}
```

## 3.2 新的分表算法

**位置：** `com.damai.shardingsphere.powerof2.TableOrderPowerOf2Algorithm`

```java
public class TableOrderPowerOf2Algorithm implements ComplexKeysShardingAlgorithm<Long> {
    
    /**
     * 属性：分表名
     */
    private static final String SHARDING_COUNT_KEY_NAME = "sharding-count";
    
    /**
     * 用于计算表索引的掩码
     */
    private long tableMask;
    
    @Override
    public void init(Properties props) {
        //当前表数量（必须是2的幂次方）
        final int tableCount = Integer.parseInt(props.getProperty(SHARDING_COUNT_KEY_NAME));
        
        // 验证是否为2的幂次方
        if (!isPowerOf2(tableCount)) {
            throw new IllegalArgumentException(
                String.format("表数量必须是2的幂次方（2,4,8,16...），当前: %d", tableCount)
            );
        }
        
        // 计算掩码：tableCount=8时，mask=7=0b111
        this.tableMask = tableCount - 1;
    }
    
    @Override
    public Collection<String> doSharding(
        Collection<String> allActualSplitTableNames,
        ComplexKeysShardingValue<Long> complexKeysShardingValue) {
        
        List<String> actualTableNames = new ArrayList<>();
        String logicTableName = complexKeysShardingValue.getLogicTableName();
        Map<String, Collection<Long>> columnNameAndShardingValuesMap = 
            complexKeysShardingValue.getColumnNameAndShardingValuesMap();
        
        // 如果没有条件查询，返回空（不允许全表扫描）
        if (CollectionUtil.isEmpty(columnNameAndShardingValuesMap)) {
            return actualTableNames;
        }
        
        // 获取分片键
        Long shardingKey = getShardingKey(columnNameAndShardingValuesMap);
        
        if (Objects.nonNull(shardingKey)) {
            // ⭐ 核心：使用位运算计算表索引
            long tableIndex = shardingKey & tableMask;
            actualTableNames.add(logicTableName + "_" + tableIndex);
            return actualTableNames;
        }
        
        // 没有分片键，返回所有表（允许特殊场景）
        return allActualSplitTableNames;
    }
    
    /**
     * 获取分片键（优先order_number，其次user_id）
     */
    private Long getShardingKey(Map<String, Collection<Long>> columnMap) {
        //order_number条件的值
        Collection<Long> orderNumberValues = columnMap.get("order_number");
        //user_id条件的值
        Collection<Long> userIdValues = columnMap.get("user_id");
        //如果是order_number查询
        if (CollectionUtil.isNotEmpty(orderNumberValues)) {
            return orderNumberValues.stream().findFirst()
                .orElseThrow(() -> new DaMaiFrameException(BaseCode.ORDER_NUMBER_NOT_EXIST));
        } else if (CollectionUtil.isNotEmpty(userIdValues)) {
            //如果是user_id查询
            return userIdValues.stream().findFirst()
                .orElseThrow(() -> new DaMaiFrameException(BaseCode.USER_ID_NOT_EXIST));
        }
        return null;
    }
    
    /**
     * 判断是否为2的幂次方
     */
    private boolean isPowerOf2(int n) {
        return n > 0 && (n & (n - 1)) == 0;
    }
}
```

# 四、如何扩容

## 4.1 扩容前准备

### 第1步：修改配置（准备扩容到2库×8表）

```yaml
# application.yml

rules:
  - !SHARDING
    tables:
      d_order:
      	# 扩容到8张表
        actualDataNodes: ds_${0..1}.d_order_${0..7}
        databaseStrategy:
          complex:
            shardingColumns: order_number,user_id
            shardingAlgorithmName: databaseOrderComplexGeneArithmetic
        tableStrategy:
          complex:
            shardingColumns: order_number,user_id
            shardingAlgorithmName: tableOrderComplexGeneArithmetic
    shardingAlgorithms:
      databaseOrderComplexGeneArithmetic:
        type: CLASS_BASED
        props:
        	# 分库数量不变
          sharding-count: 2
          # ⬆️ 分表数量从4改为8
          table-sharding-count: 8
          strategy: complex
          algorithmClassName: com.damai.shardingsphere.powerof2.DatabaseOrderPowerOf2Algorithm
      tableOrderComplexGeneArithmetic:
        type: CLASS_BASED
        props:
        	# ⬆️ 分表数量从4改为8
          sharding-count: 8
          strategy: complex
          algorithmClassName: com.damai.shardingsphere.powerof2.TableOrderPowerOf2Algorithm              
```

### 第2步：创建新表

```sql
-- 在 damai_order_0 库创建新表
USE damai_order_0;
DROP TABLE IF EXISTS `d_order_4`;
CREATE TABLE d_order_4 LIKE d_order_0;
DROP TABLE IF EXISTS `d_order_5`;
CREATE TABLE d_order_5 LIKE d_order_0;
DROP TABLE IF EXISTS `d_order_6`;
CREATE TABLE d_order_6 LIKE d_order_0;
DROP TABLE IF EXISTS `d_order_7`;
CREATE TABLE d_order_7 LIKE d_order_0;

-- 在 damai_order_1 库创建新表
USE damai_order_1;
DROP TABLE IF EXISTS `d_order_4`;
CREATE TABLE d_order_4 LIKE d_order_0;
DROP TABLE IF EXISTS `d_order_5`;
CREATE TABLE d_order_5 LIKE d_order_0;
DROP TABLE IF EXISTS `d_order_6`;
CREATE TABLE d_order_6 LIKE d_order_0;
DROP TABLE IF EXISTS `d_order_7`;
CREATE TABLE d_order_7 LIKE d_order_0;
```

### 第3步：调整订单编号生成时的分表数量

**位置：** `com.damai.constant.ProgramOrderConstant#ORIGINAL_TABLE_COUNT`

```java
/** 分表数量从4改成8 */
public static final int ORIGINAL_TABLE_COUNT = 8;
```

#### 使用新的分表数量后，新的订单编号生成逻辑：

```java
// ⚠️ 新订单使用新的基因位数（4位）
public synchronized long getOrderNumber(long userId, long tableCount, long databaseCount) {
    // tableCount = 8, databaseCount = 2
    long tableGeneLength = log2N(tableCount);      // 3位
    long databaseGeneLength = log2N(databaseCount); // 1位
    long totalGeneLength = tableGeneLength + databaseGeneLength; // 4位
    
    long geneMask = (1L << totalGeneLength) - 1;  // 0b1111
    long userGene = userId & geneMask;
    
    return ((timestamp - BASIS_TIME) << timestampLeftShift)
            | (datacenterId << datacenterIdShift)
            | (workerId << workerIdShift)
            | (sequence << totalGeneLength)  // ⬆️ 左移4位
            | userGene;
}
```

## 4.2 执行扩容

 

- 先启动 migrate-service 服务

- 调用执行扩容接口：

  ```
  http://127.0.0.1:6085/damai/migrate/order/data/power/of/2/expand
  ```

## 4.3 验证扩容

- 调用验证库容接口

  ```
  http://127.0.0.1:6085/damai/migrate/order/data/power/of/2/verify
  ```

# 五、扩容策略选择

## 策略A：停写扩容（推荐）

```
时间：凌晨2:00-4:00（低峰期）

步骤：
1. 23:59 发布公告：系统将于2:00-4:00维护
2. 02:00 停止订单服务（nginx拦截）
3. 02:05 执行数据迁移（约1-2小时）
4. 03:30 验证数据完整性
5. 03:45 启动服务，切换到新配置
6. 04:00 开放订单功能

优点：✅ 实现简单，数据一致性有保证
缺点：⚠️ 需要停服2小时
```

## 策略B：双写扩容（零停机）

```
阶段1：准备期（1天）
- 创建新表
- 部署双写逻辑（写旧表+写新表）

阶段2：迁移期（数小时）
- 迁移旧数据到新表
- 持续双写新订单

阶段3：验证期（1天）
- 验证新表数据完整性
- 对比新旧表数据

阶段4：切换期（秒级）
- 读写切换到新表
- 停止双写

优点：✅ 零停机，业务无感知
缺点：⚠️ 实现复杂，需要双写逻辑
```

# 六、常见问题 FAQ

## Q1：为什么必须是2的幂次方？

**A**：因为位运算 `(n-1) & value` 只有在n是2的幂次方时，才等价于 `value % n`。

```
n=8时：n-1=7=0b111，可以取value的低3位
n=7时：n-1=6=0b110，无法均匀分布数据
```

## Q2：能否从4表直接扩容到16表？

**A**：理论上可以，但**不推荐**。

```
4表→8表：每表拆分为2个，迁移50%数据
4表→16表：每表拆分为4个，迁移75%数据

风险：
- 迁移数据量大，耗时长
- 出错概率增加
- 回滚困难

建议：
- 逐步扩容：4→8→16→32
- 每次只翻倍
```

## Q3：能否只扩容部分表？

**A**：**不能**！必须所有表同时扩容。

```
原因：
- 位运算算法是全局统一的
- 如果只扩容部分表，会导致数据路由错误

例如：
- d_order_0 扩容为 d_order_0 + d_order_4
- 但 d_order_1 不扩容
→ userId=5 的数据会路由到 d_order_5（不存在）
```

## Q5：订单号生成需要修改吗？

**A**：需要调整基因位数，但**旧订单号仍然有效**。

```java
// 扩容前：3位基因（1位库 + 2位表）
long totalGeneLength = 3;
long userGene = userId & 0b111;

// 扩容后：4位基因（1位库 + 3位表）
long totalGeneLength = 4;
long userGene = userId & 0b1111;

旧订单号兼容性：
- 旧订单号：...001 → 扩容后路由到 d_order_1（正确）
- 新订单号：...0101 → 路由到 d_order_5（正确）

⚠️ 关键：扩容时必须同时更新订单号生成器！
```
